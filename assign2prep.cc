#include<iostream>
#include<stdio.h>


class point{

public:
    point();
    float x;
    float y;
    float z;
};

class pointh{

public:
    pointh();
    float x;
    float y;
    float z;
};

multiply(point pointReturn, float scalar, point p){
    pointReturn.x = point.x * scalar;
    pointReturn.y = point.y * scalar;
    pointReturn.z = point.z * scalar;
    return(pointReturn);
}


int main(){
    // Test 1

    return(0);
}